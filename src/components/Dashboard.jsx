import React from "react";

const Dashboard = () => {
  return (
    <div className="dashboard">
      <h1>Admin Dashboard</h1>
      <div>
        <p>
          Welcome to the admin dashboard. Use the navigation links to manage
          categories, questions, students, and access codes.
        </p>
      </div>
    </div>
  );
};

export default Dashboard;
